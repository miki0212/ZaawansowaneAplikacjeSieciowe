export class StartPageModules {
    constructor() {
        this.render = (mainCointainer) => {
            const baseContainer = document.createElement('div');
            const usernameNode = document.createElement('input');
            const string = `
            <div></div>
        `;
        };
    }
}
